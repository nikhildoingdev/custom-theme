<h1>Search Results for {{ search.terms }}</h1>

{% if search.types == "Product" %}
  {% for item in search.results %}
  <li>{{ item.title }}</li>
  {% endfor %}
{% endif %}

<div>
  {% for type in search.types %}
    {{ type }}
  {% endfor %}
</div>